import { test } from 'node:test';
import assert from 'node:assert';
import { FUNCTION_NAME } from '../challenges/CHALLENGE_NAME.js';

test('CHALLENGE_NAME - caso básico', () => {
  // Arrange
  const input = null; // TODO: Definir input
  const expected = null; // TODO: Definir resultado esperado
  
  // Act
  const result = FUNCTION_NAME(input);
  
  // Assert
  assert.strictEqual(result, expected);
});

test('CHALLENGE_NAME - caso edge', () => {
  // TODO: Agregar más casos de prueba
  assert.ok(true);
});

